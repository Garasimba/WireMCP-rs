[package]
name = "monitor-scan"
version = "1.0.0"
edition = "2021"

[dependencies]
pcap = "2"
clap = { version = "4", features = ["derive"] }
ctrlc = "3"
libc = "0.2"

[profile.release]
opt-level = 3
lto = true
strip = true
